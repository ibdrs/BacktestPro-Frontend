services:
  frontend:
    image: ghcr.io/ibdrs/backtestpro-frontend:latest
    build:
      context: .
      args:
        VITE_API_URL: ""
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "80:8080"
    restart: unless-stopped
